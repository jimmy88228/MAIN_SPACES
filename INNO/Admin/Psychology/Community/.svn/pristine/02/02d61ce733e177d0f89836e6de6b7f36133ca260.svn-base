import {
    MainApi,
    WebApi
} from "./http-manager";
export const installHttp = {
    install(Vue) {
        Vue.prototype.$MainApi = MainApi;
        Vue.prototype.$WebApi = WebApi;
    }
}