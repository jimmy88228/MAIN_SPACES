var progressVal = 0;

var timeupdate = function (event, ownerInstance) {
  // var dataset = event.currentTarget.dataset || {};
  // var detail = event.detail || {};
  // progressVal = parseInt((detail.currentTime / detail.duration) * 100);
  // var instance = ownerInstance.selectComponent('.progress');
  // instance.setStyle({
  //   width: progressVal + "%",
  // })
} 

module.exports = {
  update: timeupdate,
}