// 自定义公共组件
import optionalArea from "@/views/main-components/view-components/optional-area";
import optional from "@/views/main-components/view-components/optional";
import dataSelect from "@/views/main-components/view-components/data-select";

function installComm(Vue){
	if(Vue){
		Vue.component(optionalArea.name, optionalArea);
		Vue.component(optional.name, optional);
		Vue.component(dataSelect.name, dataSelect);
	}
}

export {
	installComm
}