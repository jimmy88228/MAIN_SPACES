var slideChange = function (event, ownerInstance){
  var detail = event.detail || {};
  var instance = ownerInstance.selectComponent('#movePoint');
  instance.setStyle({
    transform: "translateX(" + (detail.x || 0) + "px)"
  })
}
module.exports = {
  slideChange: slideChange
}