<view class="membership_card_main" hidden='{{showImg}}'>
   <view class="protocol">
    <!-- <navigator class="protocol_link" url="./card_detail">
      <image class="tip_icon" src="{{brand_info.icon_url}}micro_mall/membership_card/node_icon.png" mode="widthFix"></image>
      <text class="protocol_text {{isAndroid ? 'android' : ''}}">卡片详情</text>
    </navigator> -->
  </view> 
  <view class='logos'>
    <image src="{{rankImage}}" class='logo_pic' binderror="errAction" mode='aspectFit'/>
  </view>
  <view class="code_img_box {{!rankImage ? 'center' : ''}}">
    <canvas canvas-id="barCanvas" class="barCanvas"></canvas>
    <view class="code_text">{{cardNo}}</view>
    <view class="use_tip">使用时出示此码</view>
  </view>
</view>