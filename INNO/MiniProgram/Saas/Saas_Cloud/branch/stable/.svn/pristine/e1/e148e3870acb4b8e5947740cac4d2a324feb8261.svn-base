let PromotionLabels = {};
let goodsIds = [];

function extractLabelInfo(goods_id){
  
}

export function addGoods(goods_id){
  if (!goods_id) return Promise.reject();
  goodsIds.push(goods_id)
  return 
}