export default {
  authorized: 0, // 授权
  bindPhone: 0, // 绑定手机号
  fullfillProfile: 0, // 完善资料

  isLogin() {
    return (this.authorized === 1 && this.bindPhone === 1 && this.fullfillProfile == 1) ? 1 : 0
  }
}